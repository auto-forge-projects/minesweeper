# DL-05-001: Modül ayrımı — saf oyun çekirdeği + DOM adaptörü + controller

- Tarih: 2026-07-26
- Faz: 5 — Mimari Tasarım
- Mod: AUTOPILOT
- Karar: Kod üç modüle ayrılır: `src/board.js` (saf oyun kuralları, DOM'a hiç dokunmaz), `src/render.js` (yalnız DOM yazımı, oyun kuralı bilmez), `src/app.js` (olayları bağlar, ikisini birleştirir). Stil tek `styles.css` dosyasında (CSS Grid) kalır.
- Değerlendirilen alternatifler: (1) tek dosyada karma mantık+DOM (en az dosya); (2) hücre-başına bileşen/sınıf hiyerarşisi (aşırı mühendislik).
- Gerekçe: NFR-2 (%100 doğru komşu sayımı) ve NFR-4 (ilk tık güvenliği) yalnız çekirdek DOM'suz ve saf olursa tarayıcısız/bağımlılıksız birim testle kanıtlanabilir. Karma tek dosya bu iddiaları jsdom/tarayıcı otomasyonuna mahkûm eder (NFR-3'ün sıfır-bağımlılık hedefiyle çelişir).
- Riskler: Üç dosya arası sözleşme kayması → arayüz tablosu (`docs/05-architecture.md`) tek referans; Faz 9 imzaları birebir uygular, Faz 10 sapmayı denetler.
- Geri alınabilirlik: Yüksek (üç küçük dosya; birleştirme mekanik bir refactor, tersine çevirmek de öyle).
- İnsan onayı: Otomatik
- Varsayım mı?: Hayır
